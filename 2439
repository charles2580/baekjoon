#include <stdio.h>


int main()
{
	int N;
	scanf("%d", &N);

	for (int i = 0; i < N; i++)
	{
		for (int l = N; l > i+1; l--)
		{
			printf(" ");
		}
		for (int z = 0; z < i+1; z++)
		{
			printf("*");
		}
		printf("\n");
	}
}
