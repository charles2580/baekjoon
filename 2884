#include <stdio.h>

int main()
{
	int h, m;

	scanf("%d %d", &h, &m);


	if (m - 45 < 0)
	{
		if (h == 0)
		{
			printf("23 %d", 15 + m);
		}
		else
		{
			printf("%d %d", h - 1, 15 + m);
		}
	}
	else
	{
		printf("%d %d", h, m - 45);
	}
}
